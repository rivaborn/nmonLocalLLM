<!--
  Generated by dataflow_local.ps1
  Date:      2026-04-19 21:53
  Codebase:  nmon -- a Python terminal system/GPU monitor. Collects GPU metrics via NVML and nvidia-smi, stores samples in SQLite, and renders a live TUI (Rich-based) with a dashboard, history view, and configurable widgets. Stack: Python 3.10+, rich, pynvml, readchar, TOML config, pytest test suite.
  Files:     17 / 17 extracted
  LLM:       qwen3-coder:30b
-->
# Data Flow: nmon

## Pipeline Overview

  [main.py]  -->  [app.py]  -->  [gpu_monitor.py]  -->  [db.py]  -->  [views/...]
  (Settings)        (NmonApp)        (GpuSnapshot)        (DbConnection)        (Layout)
  
  [main.py]  -->  [app.py]  -->  [ollama_monitor.py]  -->  [db.py]  -->  [views/...]
  (Settings)        (NmonApp)        (OllamaSnapshot)        (DbConnection)        (Layout)

## Shared Data Types

- GpuSnapshot: gpu_index: int, timestamp: float, temperature_c: float, mem_junction_temp_c: Optional[float], memory_used_mb: float, memory_total_mb: float, power_draw_w: float, power_limit_w: float  |  defined in: gpu_monitor.py
- OllamaSnapshot: timestamp: float, reachable: bool, loaded_model: Optional[str], model_size_bytes: Optional[int], gpu_use_pct: Optional[float], cpu_use_pct: Optional[float], gpu_layers: Optional[int], total_layers: Optional[int]  |  defined in: ollama_monitor.py
- Settings: app_name: str, log_level: str, db_path: str, prefs_path: str, gpu_monitor_enabled: bool, temp_monitor_enabled: bool, power_monitor_enabled: bool, llm_monitor_enabled: bool  |  defined in: config.py
- UserPrefs: refresh_rate: int, theme: str, show_gpu: bool, show_temp: bool, show_power: bool, show_llm: bool  |  defined in: config.py
- AlertState: active: bool, message: str, color: str, expires_at: float  |  defined in: alerts.py
- HistoryStore: _settings: Settings, _gpu_snapshots: deque[GpuSnapshot], _ollama_snapshots: deque[OllamaSnapshot]  |  defined in: history.py

## Stage-by-Stage Flow

### Stage 1: Configuration Loading (config.py)

| | |
|---|---|
| **Input** | None |
| **Processing** | Loads application settings and user preferences from files |
| **Output** | Settings, UserPrefs |
| **Thread** | Main thread |
| **Errors** | json.JSONDecodeError, IOError when reading files |

### Stage 2: Application Initialization (main.py)

| | |
|---|---|
| **Input** | Settings, UserPrefs |
| **Processing** | Initializes database, monitors, and history store |
| **Output** | NmonApp |
| **Thread** | Main thread |
| **Errors** | SystemExit(1) on pynvml init failure, Exception on app.run() failure |

### Stage 3: Data Collection (app.py)

| | |
|---|---|
| **Input** | NmonApp |
| **Processing** | Polls GPU and Ollama monitors for new samples |
| **Output** | Tuple[List[GpuSnapshot], OllamaSnapshot] |
| **Thread** | Main thread |
| **Errors** | Exception in Ollama probe |

### Stage 4: GPU Data Collection (gpu_monitor.py)

| | |
|---|---|
| **Input** | None |
| **Processing** | Uses NVML to collect GPU metrics for all GPUs |
| **Output** | List[GpuSnapshot] |
| **Thread** | Main thread |
| **Errors** | Various pynvml exceptions caught and logged |

### Stage 5: Ollama Data Collection (ollama_monitor.py)

| | |
|---|---|
| **Input** | Settings |
| **Processing** | Makes HTTP requests to Ollama API to get model metrics |
| **Output** | OllamaSnapshot |
| **Thread** | Main thread |
| **Errors** | httpx.TimeoutException, httpx.RequestError |

### Stage 6: Data Storage (db.py)

| | |
|---|---|
| **Input** | List[GpuSnapshot], OllamaSnapshot |
| **Processing** | Flushes collected samples to SQLite database |
| **Output** | None |
| **Thread** | Main thread |
| **Errors** | Exception during flush, logged and ignored |

### Stage 7: Data Rendering (views/...)

| | |
|---|---|
| **Input** | GpuSnapshot, OllamaSnapshot |
| **Processing** | Renders dashboard views with collected data |
| **Output** | Layout |
| **Thread** | Main thread |
| **Errors** | None specified |

## Thread Boundaries

- Boundary: main thread -> main thread
- Data: GpuSnapshot, OllamaSnapshot
- Protection: None (no explicit thread synchronization)

## Handoff Points: Debugging Guide

### main.py -> app.py
- **Function:** NmonApp.__init__()
- **Type in transit:** Settings, UserPrefs
- **What to check:** Ensure config files are readable and valid
- **Failure mode:** Application fails to initialize with invalid settings

### app.py -> gpu_monitor.py
- **Function:** GpuMonitorProtocol.poll()
- **Type in transit:** None
- **What to check:** Verify NVML initialization and GPU enumeration
- **Failure mode:** No GPU metrics collected, empty snapshot list

### app.py -> ollama_monitor.py
- **Function:** OllamaMonitorProtocol.poll()
- **Type in transit:** None
- **What to check:** Confirm Ollama API endpoint is reachable
- **Failure mode:** No LLM metrics collected, snapshot with reachable=False

### gpu_monitor.py -> db.py
- **Function:** flush_to_db()
- **Type in transit:** List[GpuSnapshot]
- **What to check:** Verify database connection and write permissions
- **Failure mode:** GPU metrics not persisted to database

### ollama_monitor.py -> db.py
- **Function:** flush_to_db()
- **Type in transit:** OllamaSnapshot
- **What to check:** Ensure database connection is valid
- **Failure mode:** LLM metrics not persisted to database

### db.py -> views/...
- **Function:** HistoryStore methods
- **Type in transit:** GpuSnapshot, OllamaSnapshot
- **What to check:** Verify data integrity in database queries
- **Failure mode:** Dashboard displays stale or incorrect metrics
